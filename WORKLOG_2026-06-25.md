# WORKLOG 2026-06-25

## 一、App 打包上架 TestFlight / 支付审核准备
协助完成 iOS App 从 Xcode 归档到 App Store Connect 上传、配置 TestFlight 测试与 IAP 支付审核所需信息的全流程。

### 修复上传被拒
- **缺 App 图标**(上传报 `90023` 缺 iPad 152×152 / `90713` 缺 `CFBundleIconName`)
  - 根因:`Assets.xcassets/AppIcon.appiconset` 只有 `Contents.json`、三个槽位无 `filename`,实际无 PNG。
  - 修复:用 `~/Desktop/logo.jpg`(1188×1188 无 alpha)经 `sips` 转 1024×1024 PNG;`Contents.json` 改为单张 1024 通用图标,Xcode 自动派生全尺寸并写入 `CFBundleIconName`。
- **出口合规问卷反复弹出**
  - `SupabaseConfig.plist`(项目 `INFOPLIST_FILE` 基底)新增 `ITSAppUsesNonExemptEncryption = <false/>`,后续上传不再弹加密文稿。已上传的 1.0(1) 不受影响,本次网页手动选(标准加密 / 法国分发=否)。

### TestFlight / 审核信息(网页侧指导)
- 分发方式选 **App Store Connect**(兼容 TestFlight 内/外部测试与正式上架)。
- 外部测试「测试信息 / 测试内容」均提供英文文案,聚焦 IAP 购买流程测试步骤。
- 演示账号(给审核员登录):`apple.review@yolohappy.com`,需先在 App 内用邮箱注册成真实可登录账号。
- 测试用户两类说明:演示账号(填 App 审核信息)+ Sandbox 沙盒测试员(测付款不真实扣费)。
- 关键提醒:IAP 订阅商品须在「App 内购买项目」建好并与版本一起提交,否则支付功能不过审。

## 二、封面图统一尺寸(竖图/横图宽度不一致)
- 现象:武侯祠(竖图)与文殊院(横图)景点封面看起来宽度/边距不同。经查两者均为单张封面、`cover_images` 为空,走同一渲染分支。
- 根因:`Utils/CoverImageView.swift` 用 `.frame(height:)` + `.frame(maxWidth:.infinity)` 两段式,`scaledToFill` 先按固定高把竖图撑成窄条,后续 maxWidth 不再拉满整行宽 → 竖图封面比横图窄。
- 修复:改为 `Color.clear` 占位定「满宽 × 固定高」框,图片以 `.overlay` + `scaledToFill` + `clipped` 裁剪填充。任意高宽比源图均渲染为同一尺寸;组件全局共用,列表缩略图/子区域页封面一并规整。
- 验证:`xcodebuild`(iphonesimulator)BUILD SUCCEEDED。

## 三、景点页地址模块长文本自动换行
- 需求:英文地址超长自动换行不截断;中文地址折行时复制图标跟随文字流到末尾。
- `Features/Guide/AttractionDetailView.swift`:
  - 英文地址 Text 加 `.fixedSize(horizontal:false, vertical:true)` + 满宽左对齐,强制换行。
  - 中文地址由 `HStack[Text, copyButton]` 改为 `addressLabel(_:)`——`Text(地址) + Text(图标)` 拼接,图标内联随文字换行;整行包成 Button,点任意位置即复制(图标短暂变 ✓)。
- 副产物:编译把地址里的 `"  "` 自动抽取进 `Localizable.xcstrings`。
- 验证:`xcodebuild`(iphonesimulator)BUILD SUCCEEDED。

## 四、Settings 页功能整理优化(`SettingsView.swift`)

### 问题诊断
- **中英文混排**:`Trip reminders / Cache Size / Clear * / Privacy Policy / Terms of Service / Sign Out / Delete account` 等 17 项缺 `zh-Hans` 翻译,中文环境掉回英文。
- **开发者 jargon 暴露**:`内容模式 Live·AI`、`从 CMS 刷新` 普通用户看不懂。
- **缓存清理过碎**:4 个独立按钮 + 技术化描述(HTTP 缓存/临时文件)。
- **缺分组结构**:所有项平铺,无分区与卡片。

### 改动(与用户对话确认方向)
- **视觉改 iOS 分组卡片**:按 `通知 / 偏好 / 内容 / 存储 / 支持 / 账号` 分区,每组圆角白卡(`borderLight` 描边)置于 `backgroundSubtle` 灰底;统一 `navRow / rowLabel / destructiveRowLabel` 行构件与左缩进分隔线;`›` 改用 SF Symbol `chevron.right`。
- **开发者功能保留但优化文案**:`内容模式 + 从CMS刷新` 合并为单行「检查内容更新」(副标题「获取最新景点、攻略与音频」,刷新时显示 spinner);原始 `contentModeLabel`(Live·AI)仅在 `#if DEBUG` 下作为副标题尾巴保留给开发者。
- **缓存清理合并**:4 按钮 → 单行「清除缓存」,右侧显示总大小,点击 `CacheService.clear(.all)` 后刷新并弹「已清除所有缓存内容。」。
- **补 20 条 `zh-Hans` 翻译**:设置/通知/行程提醒/偏好/内容/检查内容更新/存储/清除缓存/隐私政策/服务条款/退出登录/删除账号 等(`Clear Cache / Account / Cancel` 已有,跳过)。

### 验证
- `xcodebuild`(iphonesimulator,Debug,CODE_SIGNING_ALLOWED=NO)**BUILD SUCCEEDED**。

## 待办
- 「地址宽度不能超过封面图宽度」:结构上封面与地址同在带 `screenPadding` 的同一列,最大宽度应相等。待用户重新运行后提供同框截图,确认是封面未撑满还是地址溢出,再按「地址右边缘对齐封面右边缘」处理。
