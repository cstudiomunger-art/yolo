# WORKLOG · 2026-06-12

本日主线：登录/注册界面整体重设计（脱离系统 Form，对齐全 App 设计语言）。

---

## 一、登录/注册界面重设计

### 1. 新增共享样式组件 `YOLO/Features/Auth/AuthUI.swift`
- `AuthFieldStyle`：米白底（backgroundSubtle）+ 1pt 边框 + 4pt 圆角输入框，对齐 CountryPickerView 搜索框先例。
- `AuthPrimaryButtonStyle`：全宽 48 高、白底黑字 1pt 描边主按钮；禁用态降为 textDisabled；按压态米白底。
- `AuthDivider`：两侧细线 + 居中小字分隔（「或使用邮箱登录」）。
- `LegalLinksText`：一句话协议文案，内嵌 markdown 链接（`yolo://legal/terms|privacy`），`OpenURLAction` 拦截后以 sheet 弹出 `LegalDocumentView`——彻底解决旧版协议行「中间断开 + 两个 > 箭头」问题，服务条款/隐私政策两词金色可点。
- `legalDocumentSheet(item:)` 扩展：底部弹窗展示法律文档。

### 2. 重写 `LoginView.swift`（Form → ScrollView 自定义布局）
- 布局自上而下：Apple 按钮 → 协议小字 → 分隔线 → 邮箱/密码（注册多一个确认密码）→ 忘记密码（仅登录）/ 勾选协议行（仅注册）→ 主按钮 → 状态文案 → 底部模式切换。
- 去掉顶部 Sign In / Sign Up 分段选择器，改为页底「还没有账号？注册 / 已有账号？登录」（金色可点）。
- 注册按钮文案改为「Create Account / 创建账号」。
- 主按钮 loading 时内嵌菊花；邮件确认等待页改为米白卡片样式。
- **全部业务逻辑零改动**（提交校验、注册后自动登录、邮件确认状态机、埋点、错误转换）。

### 3. Apple 注册登录
- `AppleSignInButton`：`.signIn` → `.continue`，文案变「通过 Apple 继续 / Continue with Apple」，注册+登录一体（signInWithIdToken 首次即自动建号）。
- Apple 按钮下方新增一行协议小字「继续即表示你同意服务条款与隐私政策」（两处可点，sheet 查看），邮箱注册仍保留显式勾选。

### 4. 顺带统一 `ForgotPasswordView` / `SetNewPasswordView`
- 同样从 Form 改为自定义布局，复用 AuthUI 组件；60s 冷却、状态机逻辑不变。

### 5. `AuthLandingView`
- Logo 区顶部留白 40→48、底距 24→28，适配新表单。

### 6. 本地化 `Localizable.xcstrings`
- 新增 zh-Hans：两条协议 markdown 句、「还没有账号？」「已有账号？」「创建账号」「忘记密码？」「登录后开始规划你的中国之旅」。

---

## 二、Google 登录 + 布局再调整

### 1. Google 登录（OAuth 网页流程，零新依赖）
- 新建 `YOLO/Features/Auth/GoogleSignInButton.swift`：supabase-swift 内置 `signInWithOAuth(provider: .google, redirectTo: yolohappy://auth-callback)`，走 `ASWebAuthenticationSession`，无需引入 Google SDK、无需注册 URL Scheme。
- 用户取消（`canceledLogin`）静默处理不报错；成功埋点 `sign_in_google`。
- 按钮样式：白底 + 1pt 黑描边 + 蓝色 G + 「通过 Google 继续」，与 Apple 按钮同高同圆角（Apple 按钮 clip 成 8pt 圆角统一）。
- **后台前提**（未配则点击报错）：Google Cloud Console 建 OAuth Web 客户端（授权重定向 URI 填 `https://<项目ref>.supabase.co/auth/v1/callback`）→ Supabase Dashboard → Auth → Providers → Google 填 Client ID/Secret 启用 → Auth → URL Configuration → Redirect URLs 加 `yolohappy://auth-callback`。

### 2. 布局调整
- 登录模块（Apple/Google/邮箱整体）下移 72pt；Logo 区顶距 48→64。

---

---

## 三、登录页改为「三按钮选择页」+ 邮箱自动注册

### 1. 第一屏三按钮选择
- `LoginView` 重构为纯选择页：Apple / Google / 邮箱三个**统一白底浅灰描边**按钮（8pt 圆角，48 高）。
  - Apple 按钮由 `.whiteOutline`（深边）改 `.white` + 自绘浅灰描边，与 Google/邮箱完全统一（`SocialButtonChrome` modifier）。
  - 邮箱按钮：envelope 图标 + 「使用邮箱登录」，点击弹 `EmailAuthSheet`。
- 布局：Logo 顶部（顶距 80）+ `Spacer` 把三按钮推到屏幕底部。
- **显式协议同意**：三按钮下方放圆圈勾选行「我已阅读并同意 服务条款 与 隐私政策」（两词金色可点弹 sheet）；未勾选时三按钮 `.disabled` + 置灰（opacity 0.4），勾选后才可点。取代原「继续即表示同意」隐式小字。

### 2. 邮箱自动注册（新建 `EmailAuthSheet.swift`）
- 底部 sheet 弹窗，单一「继续」流程：先 `signIn`，失败且为 invalid-credentials 则 `signUp` 自动注册。
  - 已注册但密码错（signUp 返回混淆 user，`isAlreadyRegisteredUser`）→ 提示「邮箱或密码错误」。
  - 已注册未确认邮箱（signIn 返回 email-not-confirmed）→ 进确认提示。
  - 新注册成功且需确认 → 弹「账号已创建，请前往邮箱点击确认链接」（保持邮箱验证，防滥用）。
- 密码框 `AuthPasswordField`：单框 + 眼睛图标显示/隐藏切换（合并流程无法用「确认密码」防错）。
- 内含邮箱、密码、忘记密码（push）、继续按钮。

### 3. 文案
- 新增 zh-Hans：使用邮箱登录、返回、显示/隐藏密码、「输入邮箱和密码…自动创建账号」、「账号已创建…确认链接」、「请前往邮箱点击确认链接以完成登录」。

---

## 四、游客模式（App Store 5.1.1(v) 合规）+ 协议抖动提醒

### 1. 游客模式（"先逛逛"）
- 之前是硬登录墙（`RootView` 未登录直接挡在 `AuthLandingView`），有 5.1.1(v) 被拒风险（攻略浏览等无账户功能也被锁）。
- `UserPreferencesStore.isGuestMode`（持久化 `yolohappy.guestMode`，`resetAll()` 登出时清除）。
- `RootView` gating 改为 `!isAuthenticated && !isGuestMode → AuthLandingView`；游客同样走 intro/nationality/notification onboarding 后进 MainTab。
- 登录门户底部新增「先逛逛」入口（`LoginView(showGuestOption:)`，仅登录门户显示，Profile 二次登录 sheet 不显示）。
- **按需登录拦截**：游客点击账户级操作时弹登录 sheet（`loginSheet` 复用 modifier）——
  - AI 助手（`HomeView.submitAI`）
  - 会员订阅（`MembershipCenterView` 的 PlanSummaryRow）
  - 景点解锁购买（`MembershipPlansView.purchaseSelected`）
- 登录成功后 `isAuthenticated` 优先，游客标志失效；`syncAfterSignIn` 把游客期本地数据并入账户。

### 2. 协议同意改为「抖动提醒」
- 三个登录按钮不再因未勾选而置灰禁用，改为正常可点。
- 未勾选时点任意按钮：透明 overlay 拦截点击 → 协议行 `ShakeEffect` 水平抖动提醒；勾选后 overlay 消失，三按钮正常工作。

---

## 五、验证
- `xcodebuild build` 多次 **BUILD SUCCEEDED**，xcstrings JSON 校验通过。
- iPhone 17 模拟器实测（重置钥匙串清旧会话后）截图核对：三按钮统一浅灰描边、靠底部布局、勾选行 + 「先逛逛」在按钮下方、邮箱 sheet 全中文（密码框眼睛开关、自动注册说明、继续按钮置灰逻辑）。
- 游客模式：置 `guestMode=true` 重启，RootView 正确放行到 onboarding（不再卡登录页），证明游客可进入 app 浏览。

## 六、点击热区修复
- **引导页底部按钮**（`OnboardingPagerView`）：`Next`/`Get Started` 原先把 `.frame/.background` 加在 Button 外层，热区只在文字上。改为样式移入 label + `.contentShape(Rectangle())` + `.buttonStyle(.plain)`，整个黑色按钮区域可点。
- **编辑资料头像**（`EditProfileView`）：原先只有右下角 26pt 相机角标能点（`ProfileAvatarButton` 内部 Button 拦截了头像点击）。改为整个头像区域包进 `PhotosPicker`，`ProfileAvatarButton` 加 `.allowsHitTesting(false)` + `.contentShape(Rectangle())`，点头像任意处都能换图。

## 八、删除首页 AI 对话助手（仅 App 端，不动 Supabase 后端）

需求确认：删除整个「AI 对话助手」功能的 App 端代码（含首页输入框入口 + Plan 行程「Ask Assistant」入口）；**后端 Supabase（表、migration、`ai-complete` 函数、admin）完全不动**——`ai-complete` 仍保留 `assistant_chat` 分支与 `assistant_*` 表，只是 App 不再调用。

### 1. 删除的入口与界面
- `YOLO/Features/Home/HomeView.swift`：移除 `aiInputBar`（"✨ Ask me anything about China…" 输入框）、`submitAI()`、`aiInput`/`showLogin` 状态与 `.loginSheet`（该登录拦截仅服务于 AI 入口）。
- `YOLO/Features/Assistant/` 整个目录删除（`AssistantView` 聊天界面）。`EmergencyView` 原寄居于此且被独立使用，已抽到新文件 `YOLO/Features/Emergency/EmergencyView.swift` 保留。
- `YOLO/Features/Plan/ExperienceSuggestionsDayCard.swift`：移除 `onAskAssistant` 闭包、「Ask Assistant →」按钮，提示文案改为「These are experience ideas — no specific venues.」（体验点子展示保留）。
- `YOLO/Features/Plan/PlanFlowViews.swift`、`PlanCreateFlowView.swift`：移除卡片调用处的闭包与 `askAssistantForExperience(...)` 私有方法。

### 2. 导航
- `YOLO/App/AppNavigation.swift`：`AppModal` 删 `.assistant` case 与 `presentAssistant(...)`；`.emergency` 保留。
- `YOLO/App/MainTabView.swift`：`fullScreenCover` 删 `.assistant` 分支。

### 3. 服务层
- `YOLO/Services/AIService.swift`：删 `chatAssistant`、`chatAssistantStream`（SSE）、`offlineHint`、`loginRequiredHint` 及 `AssistantChatRequest`/`ChatHistoryItem`/`AssistantChatResponse`/`SSEChunk` 等聊天专用类型。**保留** `generateItinerary`/`planningItineraryCard`（行程 AI 生成仍在用）及 `isAuthenticated`。

### 4. 数据层
- `Repositories`（协议 + Bundled/Caching/Remote）：删 `fetchAssistantReply`、`fetchAssistantChips`。
- `YOLO/Models/DomainModels.swift`：删 `AssistantReply`、`AssistantChip` 结构。
- `YOLO/Services/ContentCacheStore.swift`：删 `assistantChips()`、`assistantReply(scenarioId:)` 缓存键。
- 删 bundle 资源 `YOLO/Resources/Static/assistant_replies.json`、`assistant_chips.json`。

### 5. 文案
- `YOLO/Features/Home/AboutChinaGoView.swift`：删 About 列表里的「Assistant」行。
- `YOLO/Services/PurchaseService.swift`：会员权益项「Advanced AI travel assistant」→「AI-powered itinerary planning」（行程 AI 仍是付费卖点）。

### 6. 刻意保留（降低风险）
- `AppBranding.assistantGreetingGeneral/Planning`、`AISettings.systemPromptAssistant` 等配置字段：嵌在核心 Codable / 对应后端 `app_settings` 列，删除需改十余处解码点且与未动的后端冲突，属良性死配置，**保留**。
- `Localizable.xcstrings` 中助手相关旧键未清理（残留无害）。

### 7. 验证
- `xcodebuild ... build` **BUILD SUCCEEDED**；全局 grep 确认无 `AssistantView/presentAssistant/chatAssistant/AssistantReply/AssistantChip/...` 残留引用。

---

## 九、代码质量重构（代码审查后续，不含测试）

背景：全库审查结论「整体健康，四处技术债」。本次处理除补测试外的全部项。

### 1. 拆分 DomainModels.swift（1491 行 → 8 个领域文件）
纯机械移动（行号区间切分），零逻辑改动：
- `CityModels.swift`（City/CityRoute/CityGuide/PracticalInfoItem）
- `AttractionModels.swift`（Attraction/NearbyPlace/FavoriteAttractionRecord/SubArea/SubAreaContentBlock）
- `AudioModels.swift`（AudioGuide/AudioSegment）
- `ChecklistModels.swift`（ChecklistItem 及类型）
- `GuideContentModels.swift`（Shopping/Reading/Hotel/HomeTip/CultureTip）
- `TravelSafetyModels.swift`（VisaRule/EmergencyData 等）
- `ItineraryModels.swift`（SampleItinerary/ItineraryDay/ItineraryActivity/ContentItineraryRow）
- `AppConfigModels.swift`（AppBranding/AISettings/AppSettingsRemote/AppSettingsRow/PaywallCopy/FlightPlatform）

### 2. 抽取 Plan 模块重复逻辑
- 新建 `PlanItineraryHelpers.swift`：`PlanAddAttractionContext`、`PlanTripCities`（从 PlanCreateFlowView 顶部移入）+ 新增 `PlanItineraryHelpers.attractionCache(for:content:)`（原两处逐字重复的 loadAttractionCache）与 `activity(from: Attraction)`（原两处重复的 ItineraryActivity 构造）。
- `PlanCreateFlowView` 两个 `planDateSheet` 重载合并为一（起始日期改用 `Date()...Date.distantFuture`，行为等价）。

### 3. 顺带修复两个真 bug（行程日期丢失）
- `PlanFlowViews.currentItinerary` 重建 SampleItinerary 时未带 `startDate/endDate` → 在详情页任何编辑/排序保存都会**抹掉行程起止日期**（影响行程状态三态判断与倒计时）。已补。
- `PlanCreateFlowView.removeActivity/appendAttraction` 同样丢日期 → review 页增删景点后保存的行程无日期。改用新增的 `SampleItinerary.withDays(_:)`（保留 share 状态与起止日期的复制器，加在 ItineraryModels.swift）。

### 4. 高频路径吞错上报遥测
`(try? await ...) ?? []` 改 do/catch + `TelemetryService.recordError`（UI 兜底行为不变）：首页 reload（checklist/cities）、行程详情 cities、创建流程 cities、景点选择器 attractions。

### 5. UserPreferencesStore：加 MARK 分节，刻意不拆文件
方法依赖 `private` 存储属性与 `suppressSyncNotification` 跨切面状态，跨文件 extension 须放宽 `private(set)` 为模块可写——用真实封装换表面整洁，不做。未来正解是按领域拆独立 store（架构级，另立项）。

### 6. 验证
每步 `xcodebuild build` **BUILD SUCCEEDED**（共三轮：模型拆分后、Plan 重构后、全部完成后）。

---

## 十、签证查询器设计方案（产出文档，待开发）

依据早十点会议纪要完成签证功能产品设计，输出 `签证查询器设计方案_2026-06-12.md`：
- 规则模型扩展：visa_rules 加 4 列（transit_hours / transit_city_ids / transit_note / visa_apply_url），补齐城市维度（240h 过境免签适用区域）
- 纯函数 VisaVerdictEngine 四态判定（免签/超天数/过境免签/需办+blocker 建议）
- 三触点布局：首页 Quick Access 三格+Hero 行入口的独立检查器（实时出结果）、创建流程条件性双路线选择页（签证友好路线本地生成不二次调 AI）、行程卡签证徽章
- 实施清单 9 项 + 内容侧依赖（适用城市清单录 CMS）

---

## 七、待办
- [ ] 真机实测 Apple 登录全流程（Supabase Dashboard 配 Apple Provider：Client ID `com.chengduyuliu.yolohappy`）。
- [ ] Google 登录后台配置（Google Cloud OAuth + Supabase Google Provider + Redirect URL `yolohappy://auth-callback`）后真机实测。
- [ ] 模拟器无法自动点按，协议抖动动画、游客→AI/购买弹登录、邮箱自动注册全链路建议真机手动过一遍。
- [ ] App Store Connect 隐私标签如实勾选「邮箱地址」收集项；隐私政策写明收集存储邮箱。
