# WORKLOG · 2026-06-11

本日两条主线：① 全代码库审查后的安全加固；② Plan 行程状态完善。

---

## 一、安全加固（审查后续）

### 1. AI 助手限登录用户使用
- `supabase/config.toml`：`ai-complete` 的 `verify_jwt` 由 `false` 改 `true`，平台层强制校验用户 JWT，游客调用被网关拒绝（堵住「公开端点可被刷火山引擎 token」）。
- `YOLO/Services/AIService.swift`：
  - `chatAssistant` 未登录直接返回「请先登录后再使用 AI 助手。」。
  - `chatAssistantStream` 改用当前会话 `access token` 做 `Authorization`（原硬编码 anon key 在 `verify_jwt=true` 下会 401）；未登录提示登录。
  - `generateItinerary` / `planningItineraryCard` 未登录跳过远程、走本地兜底行程（合理降级，避免无谓 401 往返）。
  - 新增 `isAuthenticated`（`auth.currentSession != nil`）与 `loginRequiredHint`。

### 2. 删号一并清理头像
- `supabase/functions/delete-account/index.ts`：`deleteUser` 前用 service-role client 列出 `avatars/{userId}/` 文件并 `remove`；失败不阻断删号。

### 3. 分享网页日期修复
- `web/share.html`：`payload` 列是 iOS `.convertToSnakeCase` 写入（`date_label`），原页面用驼峰 `dateLabel` 读取导致网页每天标题空白。改为优先读 snake_case（保留 camelCase 兼容），并补上体验日条目渲染。内联脚本语法已校验通过。

### 4. 数据库 RLS（迁移文件）
- `supabase/migrations/059_refund_requests_admin_access.sql`（正式，可立即上线）：修复后台退款审批失效——原 `049` 只有 `auth.uid() = user_id` 一条策略，管理员审批 update 命中 0 行；本迁移补齐管理员读写、收紧用户只能新建/查看。
- `supabase/migrations/060_protect_profile_entitlements.DRAFT.sql`（草案，**勿立即执行**）：BEFORE UPDATE 触发器，禁止普通用户 PATCH 自己的 `subscription_*` / `purchased_attraction_ids` 等实体列。**前提**：当前是模拟购买，此触发器须与「服务端写入会员状态（RevenueCat webhook）」一起上线，否则模拟购买失效——故标记 DRAFT。

### 部署须知
- iOS 改动随发版生效；两个 Edge Function 需 `supabase functions deploy ai-complete` 与 `delete-account`。
- `verify_jwt=true` 建议先发新版 App 再切（旧版助手走非流式 invoke、已登录自动带 token，不受影响）。

---

## 二、Plan 行程状态完善

需求确认（与用户对话）：三态（未开始/进行中/已结束）；按**每个行程独立起止日期**判断；Plan 卡片 + 首页倒计时区都展示；并完善通知。

### 1. 数据层（零迁移）
- DB 的 `start_date` / `end_date` 列在 `021` 已建好，本次仅让 iOS 真正写入。
- `YOLO/Models/DomainModels.swift`：`SampleItinerary` 加 `startDate` / `endDate`（编解码 + 本地持久化覆盖）。
- `YOLO/Models/UserItineraryRow.swift`：`from(trip:)` 写 `start_date` / `end_date` 列；`asSampleItinerary` 读回兜底回填。
- `YOLO/Features/Plan/PlanCreateFlowView.swift`：创建行程时写入起止日期（与 meta 同源）。

### 2. 状态计算
- `YOLO/Models/TripStatus.swift`（新建）：`TripStatus` 枚举（`upcoming` / `ongoing(day,total)` / `ended`）+ `SampleItinerary.tripStatus()`；老行程从 `meta`（"Jun 20 – Jun 27"）解析兜底，解析不出则不显示状态（回退原展示）。

### 3. 界面
- `YOLO/Features/Plan/PlanView.swift`：行程卡顶部状态胶囊徽标（`X 天后开始` / `第 X 天 / 共 N 天` / `已结束`）。
- `YOLO/Features/Home/HomeDepartureCountdownBlock.swift` + `HomeTripHeroPanel.swift`：轮播每张卡按各自行程状态显示倒计时区（未开始倒计时 / 进行中第X天 / ✓已结束）；空状态卡维持全局出发倒计时。

### 4. 通知（`YOLO/Services/TripReminderService.swift`）
- 改用行程自身起止日期排程。
- 新增结束通知：结束次日 10:00「欢迎回家 + 回顾行程」。
- 修复出发当天才创建行程收不到开始提醒的盲点（当天已过 7:30 时立即补发一次）。

### 5. 本地化
- `YOLO/Resources/Localizable.xcstrings`：新增 8 条中文翻译（纯追加，未重排）。

---

## 三、构建
`xcodebuild -scheme YOLO -destination 'iPhone 17 Pro' build` 多次 **BUILD SUCCEEDED**，0 error；`share.html` 内联脚本与 `Localizable.xcstrings` JSON 校验通过。

## 四、待办
- [ ] 接入 RevenueCat 后再启用 `060` 触发器（连同服务端写会员状态）。
- [ ] 部署两个 Edge Function；上线 `059` 退款 RLS。
- [ ] 模拟器/真机实测：行程三态显示、首页轮播每卡状态、结束通知与当天补发。
