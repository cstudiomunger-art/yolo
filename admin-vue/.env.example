# Copy to .env.local and fill with the same values as iOS Secrets.xcconfig.
# These are the public anon / publishable key — safe in the browser bundle.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key
