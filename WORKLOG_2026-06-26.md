# WORKLOG · 2026-06-26 · 官网城市指南 + Cloudflare 自动部署

接 2026-06-25 官网 v1（见 `WORKLOG_2026-06-25_官网获客站.md`）。

## 一、城市指南上官网（commit 50a2bd7）
决策：图文全免费做 SEO，**音频是唯一付费护城河**；先搭结构+管道，英文内容由 CMS 后续补，原样渲染。

- 新增 `site/scripts/fetch-content-data.mjs`：构建期拉已发布 `cities/attractions/sub_areas`，**剥离全部音频字段**（`audio_url`/`audio_guide_id` 不进快照），仅留 `has_audio` + `audio_guide_count` 做转化钩子 → `src/data/guides.json`。
- 页面：`/guides`（城市列表）· `/guides/[city]`（城市页）· `/guides/[city]/[attraction]`（景点详情：图集/简介/实用信息/外宾贴士/子区域图文/就近）。子景点除音频外全展示。
- 音频转化位：「🎧 N 段音频解说，App 内收听」引导下载。
- 验证：dist 全站 **0 个 mp3 / audio-guides URL 泄漏**。

## 二、Cloudflare 自动部署（本次）
方式：**Cloudflare 控制台 Git 集成（Workers Builds）**，与 `yoloadmin-vue` 同款。不用 GitHub Action、不用 `CLOUDFLARE_API_TOKEN`。push 到 main 即由 Cloudflare 自动构建部署。

- 配置自包含在 `site/wrangler.jsonc`：assets-only Worker「`yolo-site`」，`directory: dist`，`html_handling: auto-trailing-slash`，`not_found_handling: 404-page`。
- （先前试过 GitHub Actions 方案，CI 构建成功但缺 `CLOUDFLARE_API_TOKEN` secret，按用户选择改用控制台 Git 集成，已删除 `deploy-site.yml` 与根 `wrangler.site.jsonc`。）
- `astro.config.mjs`：`site` 改读 `SITE_URL` 环境变量（CF 构建变量），自定义域名免改代码；`Base.astro` canonical/OG 跟随 `Astro.site`。
- 新增 `site/src/pages/404.astro` 配合 `not_found_handling`；`site/package.json` 加 `deploy` 脚本（本地 `npm run deploy`）。

### Cloudflare 控制台一次性设置（用户操作）
Workers & Pages → Create → Workers → 连接本 GitHub 仓库，设：
- **Root directory**：`site`
- **Build command**：`npm run build`（构建前自动 `fetch` 拉最新 Supabase 快照）
- **Deploy command**：`npx wrangler deploy`
- 之后 push 到 main 即自动构建部署到 Worker `yolo-site`。

### 内容更新如何上线
- 本质仍是 **SSG 构建期快照**（SEO/速度/成本最优）：HTML 是 build 那一刻的数据。
- 代码 push → CF 自动重建部署；构建会重新抓取 Supabase，顺带带上最新 CMS 内容。
- **纯 CMS 内容改动**（无 push）：在 Cloudflare 项目里点 **Retry deployment / Create deployment** 手动触发一次（约 1–2 分钟生效）；后续如需「改 CMS 自动重建」可加 Supabase webhook → CF deploy hook（未做，待定）。

## 待办 / 需你操作
- 在 Cloudflare 控制台按上面设置连接仓库完成首次部署；取 URL；要自定义域名时在 CF 项目设构建变量 `SITE_URL` 并绑定域名。
- `SITE.appStoreUrl`（`site/src/lib/site.ts`）仍为占位，待真实 App Store 链接。
- 上海/苏州暂无景点、部分景点为中文占位 —— 按约定 CMS 补英文后自动出现（push 或手动触发重建）。

## 三、安全审查与加固（本次）
结论：**无匿名/远程可利用漏洞** —— 无不可信用户输入到达 HTML 注入点；anon key 只读+RLS+不进客户端包；dist 无密钥/音频/完整 guides 数据泄漏；外链均带 `rel=noopener`。以下为纵深防御加固：

- **存储型 XSS（CMS 图文）**：`set:html` 渲染的 city.description / attraction.introduction|summary / sub_area.body / content_blocks.body 改为经 `clean()`（`sanitize-html`，**仅构建期运行，不进客户端包**）清洗——标签/属性白名单，剥离 `<script>`/`onerror`/`javascript:`，外链强制 `rel=noopener nofollow ugc`。
- **客户端 innerHTML（签证 widget）**：所有来自 Supabase 的标签字段（policy/city/port/country 名、flag、headline）经 `esc()` HTML 转义后再拼入 innerHTML。
- **安全响应头**：新增 `site/public/_headers`（CF 静态资源识别）：CSP（script-src 'self'，无内联脚本已自检通过）、X-Frame-Options DENY、X-Content-Type-Options nosniff、Referrer-Policy、Permissions-Policy、COOP。
- **依赖**：`npm audit` 仅 esbuild 一项（dev-server 限 Windows 的任意文件读，GHSA-g7r4-m6w7-qqqr）——**不影响生产静态站**，需 astro 7 破坏性升级才能消除，暂不动。
- 验证：清洗器拦截 `<script>`/`onerror`/`javascript:`，正常 `<p><strong>` 保留；115 页构建通过；dist 0 内联脚本/事件处理器（CSP 兼容）。

## 四、门票工具重构（方案 2，对齐签证模式）
问题：导航里「故宫」是单个景点，与「Visa Checker / City Guides」等通用项层级不对等。改为「通用工具 + 按景点 SEO 页」，与签证「检测器 + 按国籍页」一致。

- 数据：策展文件 `src/data/ticket-attractions.json`（你可加景点/填精确放票时间；`release_time`/`advance_days` 留空则不渲染，避免编造）。`src/lib/tickets.ts` 类型化读取。
- 通用工具：`src/components/PassportNameChecker.astro` —— **护照姓名格式校验器**，纯客户端、不依赖景点数据、**用户输入只走 textContent 不进 innerHTML**（无 XSS）。
- 页面：`/attraction-tickets`（hub：校验器 + 景点网格）+ `/attraction-tickets/[slug]`（按景点：校验器 + 购票规则 + 官方链接 + 实用信息）。首版 4 个：故宫(live) / 慕田峪长城 / 兵马俑 / 成都熊猫基地(coming-soon)。
- 导航「故宫」→「Tickets」；首页第二张卡改为通用「Attraction tickets」并升级为 Live tool。
- 删除旧 `forbidden-city-tickets-foreigners.astro`；`public/_redirects` 加 301 旧 URL → `/attraction-tickets/forbidden-city`，保住 SEO。
- 验证：119 页通过，站内无残留旧链。

## 五、放票倒计时 + CSP 内联脚本修复（本次）
- **放票倒计时**：`ReleaseCountdown.astro` + `src/scripts/release-countdown.ts`，按景点 `release_time`（北京时间 HH:MM，固定 UTC+8 无夏令时，全球时区正确）倒计时到下一次放票；`advance_days` 推算"本批开放约 X 日后日期"。数据驱动：填了 `release_time` 才渲染。故宫填常见公开规则（20:00/提前 7 天）+「以官方为准」注脚，待核实微调。
- **校验器外置**：`PassportNameChecker` 脚本抽到 `src/scripts/passport-name-checker.ts`。
- **CSP 内联脚本修复（重要）**：发现 Astro 会把小体积脚本**内联**为 `<script type=module>`，而上一次安全提交(9e414d0)写在 `_headers` 的 `script-src 'self'` 会**屏蔽所有内联脚本**（含 hotels 复制按钮、新倒计时/校验器）。改为启用 **Astro `experimental.csp`**：每页 `<meta>` 注入 CSP 并**自动哈希**其内联脚本/样式 → `script-src 'self' 'sha256-…'`（无 unsafe-inline）。`_headers` 移除 CSP、保留 XFO 等。
- 配套：把 3 处内联 `style=` 改为工具类（内联样式属性无法哈希）；签证"具体行程"按钮初始隐藏改用 `.is-hidden` 类（运行时 `.style` 由 JS 改，CSP 不拦）。
- 验证：逐页校验每个内联脚本 sha256 均在该页 CSP 内（missing=0）→ 严格 CSP 下脚本可执行；0 残留内联 style 属性；119 页通过。

## 六、门票信息迁移到后台（CMS 可配置）
把门票（外宾购票页）数据从仓库 JSON 改为 **Supabase 表 + admin-vue CMS 编辑 + 站点构建期拉取**，与签证/景点一致。

- **迁移** `supabase/migrations/090_ticket_attractions.sql`：建 `ticket_attractions` 表（slug 主键 + name/name_zh/city/status/blurb/advance_days/release_time/release_note/closed/official_url/passport_required/rules(jsonb)/passport_note/display_order/is_active），RLS 公开读 active 行、管理员写（镜像 022 范式），种子 4 条。⚠️ **需手动应用**（见下）。
- **admin-vue**：`schema/tables.js` 加 `ticket_attractions` 表定义（slug/enum 状态/string_list 规则/section 分组等）；`TreeNav.vue` 侧栏新增「🌐 官网内容 → 🎟️ 门票」。push 后 yoloadmin-vue 自动重部署。
- **站点管道**：`ticket-attractions.json` 改为构建期生成（已 gitignore）；committed `ticket-attractions.seed.json` 作回退。`fetch-content-data.mjs` 拉 `ticket_attractions`，**表不存在/为空则回退种子**（迁移未应用也不会断）。验证：构建日志 `source: seed`，119 页通过。

### 应用迁移（你操作）
- `supabase db push`（会应用所有未应用迁移），或仅把 `090_ticket_attractions.sql` 粘进 Supabase 控制台 SQL Editor 执行。
- 应用后：admin-vue「官网内容 → 门票」即可增删改；官网下次构建（push 或 CF 重新部署）自动改用 Supabase 数据。

## 七、CMS 改动自动重建官网（webhook → CF deploy hook）
SSG 站点改 CMS 不重建不生效。做成「后台一改 → 自动重建」。

- 迁移 `091_site_rebuild_webhook.sql`：`pg_net` + `supabase_vault`；`public.notify_site_rebuild()`（SECURITY DEFINER）从 Vault 读 `cf_deploy_hook_url` 并 `net.http_post` 打 CF deploy hook；在站点相关表（ticket_attractions / cities / attractions / sub_areas / city_guides / culture_tips / visa_* 等）建 statement-level AFTER 触发器。
- **安全**：deploy hook URL 是密钥、且本仓库公开 → **不入库到仓库**，存 Supabase Vault（`cf_deploy_hook_url`）。Vault 未设则触发器空操作。
- 已实测 CF deploy hook 有效（POST 返回 success、build queued）。

### 启用（你操作）
1. SQL Editor 跑（**勿提交**，替换真实 URL）：`select vault.create_secret('https://api.cloudflare.com/.../deploy_hooks/XXXX', 'cf_deploy_hook_url');`
2. 应用迁移 091（`supabase db push` 或粘贴 SQL）。
3. 之后后台任一站点内容表增删改 → 自动触发 yolo-pc 重建（~1-2 分钟生效）。

## 八、全设备自适应布局
- **移动端导航**（之前的缺口：链接在 ≤760px 直接隐藏、无替代）→ 重做为**纯 CSS 汉堡菜单**（checkbox hack，无 JS → 不触碰 CSP）：≤860px 显示汉堡按钮，点开为下拉面板（5 链接 + Get the app），汉堡↔X 动画、键盘 focus 可达。
- **全局兜底**：`html{overflow-x:hidden}`、`body{overflow-wrap:break-word}`、`img/video/svg{max-width:100%}`、≤400px 收窄 `.container` 边距与按钮内距。
- 组件小屏微调：签证卡片内距、Get-the-app 暗条边距/按钮全宽等。
- **验证**（headless Chrome 多宽度实测）：mobile(390) `scrollWidth=375 ≤ 390、零元素溢出`；desktop(1280) 完整导航；汉堡菜单展开正常（链接 + CTA 全显、内容无裁切）。早前截图疑似"右侧裁切"经测为 headless 截图伪影，实际布局无溢出。

## 九、酒店模块:可筛选的外宾友好酒店目录
把 `/china-hotels-that-accept-foreigners` 从占位改为**数据驱动**（数据库 `hotels` 表，admin-vue「城市→酒店」已可编辑）。

- 抓取:`fetch-content-data.mjs` 增 hotels 拉取 → `src/data/hotels.json`(gitignore)。`src/lib/hotels.ts` 按城市分组(复用 guides 城市名)、富文本备注转纯文本、地图链接。
- 页面:**服务端渲染全部酒店(SEO 可读)+ 客户端筛选**。维度:城市 / 星级(3+/4+/5) / 每晚最高价(≤$50/100/200/400) / ✓接待外国人 / 🗣英语服务。`src/scripts/hotels-filter.ts`(外置模块,纯 show/hide,CSP 安全)实时筛选 + 计数 + 空状态 + 隐藏空城市分组。
- 卡片:名称/中文名、星级、起价、徽章、位置、地址+地图、预订链接。保留原 SEO 文案 + 双语确认模板。
- 自动重建:迁移 `092_hotels_rebuild_trigger.sql` 把 hotels 加入 091 触发器(**需应用**)。
- 验证:25 家(成都24/北京1)全渲染、2 城市分组、筛选齐;119 页通过;0 内联 style;脚本已 CSP 哈希;截图确认桌面/移动正常。
- 应用迁移:`supabase db push` 或粘贴 `092` SQL(091 的 notify_site_rebuild 函数已在)。

## 十、App 支付工具检查改为数据驱动(对齐桌面规格 `支付问题解决_逻辑_2026-06-22`)
规格反复强调"卡受理矩阵/就绪检查要数据驱动、运营改表即生效、不写死"。原 App 把这块**硬编码在 Swift**:`CardOrg` 枚举写死 5 种卡(缺 Diners/Discover)、`weChatBindingViable` 写死 `[.visa,.mc,.jcb]`、随身支付卡就绪率写死 `100/75` 且四行全 done。本次按方案 A 改为读表计算。

- **迁移 `093_payment_card_networks_checklist.sql`**:新建 `payment_card_networks`(受理矩阵,7 卡:Visa/MC/JCB 支付宝+微信均可,Amex/银联/Diners/Discover 仅支付宝)+ `payment_checklist_items`(就绪清单,4 项各 25 分,微信项 `condition=has_wechat`)。RLS 公开只读 + admin 增删改(仿 063)。**命名避坑**:规格叫 `checklist_items`,但 App 已有同名行前清单表,故新表前缀 `payment_`。
- **App Models**:新增 `CardNetwork` / `PaymentChecklistItem`,`PaymentHelperContent` 加两字段,**删除 `CardOrg` 枚举**。
- **App Service**:`cardTypes` 改 `Set<String>`(卡 id);`load()` 并行多读两表;`weChatBindingViable` 改为"所选卡有任一 `wechat_ok`";新增 `readinessPercent`(按清单 weight+condition 算)、`cardNetworks`/`checklistItems` 访问器;`bundledFallback` 补 7 卡+4 项,保证离线/mock 一致。旧本地存档(字符串数组)兼容,无需迁移。
- **App Views**:卡片多选改读 `cardNetworks`(中文名);随身支付卡用 `readinessPercent` + 遍历 `checklistItems` 渲染,微信项未达成显示"(你的卡不支持,跳过)"。取值与原 demo 一致(银联=75%、Visa=100%),但现已数据驱动。
- **后台 admin-vue**:`schema/tables.js` 加「卡受理矩阵」「就绪检查清单」两表(通用 CRUD),`TreeNav.vue` 💳 支付助手分组加两条。
- **验证**:`xcodebuild` Debug/iOS Simulator **BUILD SUCCEEDED**;`tables.js` `node --check` 通过;无残留 `CardOrg` 引用。
- **待你应用**:`supabase db push`(或粘贴 093 SQL)使两表与 seed 生效;不应用时 App 走 bundled 兜底,行为不变。
- **已应用**:093 已跑;anon REST 验证 7 卡受理值 + 4 清单项(权重/condition)全部与规格一致。

## 十一、补齐流程缺失节点(register 注册实名 + use 如何付款)
对照规格(`1_支付流程分支示意图.html` + `2_前端交接文档.md` 节点表)逐节点核流程,发现 App 准备主线缺两个规格节点:
- **register(注册实名)**:规格主线 install→**register**→bind,App 原来直接 install→bind 跳过。
- **use(如何付款)**:规格反复点名"必做的新增节点(出示付款码/扫商家码两步)",App 流程走到"随身支付卡"就结束,没有"到店怎么付"一屏。

修复(`PaymentHelperViews.swift` 的 `stepsView`,文案取自内容母本 `4_支付指引手册`):
- 插入「③ 注册账号 + 实名认证」:境外手机号/邮箱注册支付宝+微信、按护照机读区实名、微信仅 Visa/MC/JCB 且预留手机号一致。
- 插入「⑥ 到店怎么付(两步)」:被扫(出示付款码,优先)→ 主扫(扫商家码)→ 失败兜底(让商家扫你/换 App/现金)。
- 主线重编号:install② register③ bind④ verify⑤ use⑥ → card。
- 现流程与规格一致:home→(q1 国家取自 onboarding)→q2 卡→q3 行程→plan→install→register→bind→verify→use→card;rescue/merchant 离线分支照旧。
- 仍存差异(非本次范围,已记录):`trouble` 排查侧门用 GeniusBar 人工入口替代;准备步骤文案仍硬编码(规格设想 `steps_by_node` CMS 驱动);`articles_by_node` 详细图文未实现。
- 验证:代码为纯追加 cardBox(复用既有 helper);模拟器构建因本机 Xcode SDK(26.5)缺对应模拟器运行时(仅装 26.4)暂未跑通,与本改动无关。

## 十二、trouble 排查侧门(绑卡内联展开)
规格 `steps_by_node.trouble` 是绑卡的"侧门"自助排查卡(失败原因→解法),原 App 仅有跳人工 GeniusBar。按规格"侧门不打断主线、处理完回原地"的意图,实现为绑卡卡片内的**内联展开**(`showTrouble` 状态):3 条排查(发卡行拒绝/姓名空格不符/3D 验证失败,内容取自 simulator trouble 屏),并保留 GeniusBar 作"还不行→找真人"升级入口。

## 十三、articles_by_node 详细图文(plan/bind/use 的📄入口)
规格在方案页反复要求挂"坐下来研究"型详细图文(自检清单/限额/实体卡/现金…),原 App 完全没实现。本次补齐,CMS 驱动:
- **迁移 `094_payment_articles.sql`**:建 `payment_articles`(node_key/title/body_md/display_order/is_published/is_active),RLS 公开只读"已发布且启用"行+admin 全权。Seed 两篇挂 plan:「出发前 10 分钟自检」「该带哪些实体卡 / Wise·Revolut」(内容转录自母本)。
- **App Models/Service**:新增 `PaymentArticle`,`PaymentHelperContent` 加 `articles`;`load()` 多读一表(`.eq(is_published,true)`);新增 `articles(for:nodeKey)` 按 display_order 取。
- **App Views**:`articleSection(nodeKey)` 在 plan/bind/use 渲染「📄 详细图文」入口(NavigationLink);新增 `PaymentArticleView` + 轻量 `PaymentMarkdownView`(行级渲染:标题/列表/引用/图片 `![](url)`/段落,行内 **加粗** 走 SwiftUI AttributedString)。无文章则不渲染。
- **后台 admin-vue**:`schema/tables.js` 加 `payment_articles`(node_key 枚举 + body_md 用 textarea 让运营写 Markdown,非 Quill HTML),`TreeNav.vue` 加「详细图文」。
- **待你应用**:`supabase db push` 使 094 生效。模拟器构建同上,因本机缺 26.5 运行时暂未跑通,与改动无关。
