# WORKLOG · 2026-06-16

主线：按《四大功能实施方案_2026-06-15.md》开工。本日完成 **Wave 1 · 数据地基**（后端迁移 + Edge Function + admin CMS 注册）。

---

## 一、Supabase 迁移 062–065（新建）

对齐现有迁移范式：内容表用 `is_active`/`updated_at`/`sort_order`；公开读 RLS 用 `is_active = TRUE OR public.is_admin()` 的 DO 循环批量建；私有表 `auth.uid() = user_id`；Storage 用 bucket + objects policy（参考 005/007/012/061）。

### `062_visa_engine.sql`
- `visa_policies`（政策框架 P1–P5/L：priority/verdict/max_stay_days/area_scope/clock_rule）
- `visa_policy_grants`（国籍×政策×生效窗口，TEXT slug 主键便于 CMS 创建）
- `city_visa_tags`（city_id×region_type mainland/hk/macao/special）
- `entry_ports`（口岸×transit_240h/is_hainan）
- `visa_rule_overrides`（国籍×region 逐城覆盖）
- 起步种子：6 国（UK/US/FR/DE/SG/JP）政策 grants + 6 城标签（均 mainland）+ 8 口岸（含海口海南、广深 240h）。**注释标明需法务核对**。保留 `visa_rules` 作回退。

### `063_payment_helper.sql`
- `payment_advice_rules`（dimension sms/card/cash + match_json 匹配条件）、`payment_merchant_phrases`、`payment_helper_links`
- `profiles` 加列 `payment_card_types text[]`、`payment_trip_type`
- 种子：4 条商家短语 + 5 条建议规则（短信默认/印巴改邮箱、无 Visa/MC/JCB 跳过微信、偏远/城市现金）

### `064_support_chat.sql`
- `support_agents`（user_id FK、status online/busy/offline、档案）、`support_conversations`（agent_id、context_json、priority normal/emergency）、`support_messages`（body_original/body_translated/image_url）
- 辅助函数 `is_support_agent()`、`can_access_conversation()`（SECURITY DEFINER）；会话参与者/紧急抢接 RLS
- Storage bucket `chat-images`（私有，认证可读写）
- 三表加入 `supabase_realtime` 发布（幂等异常捕获）
- `app_settings` 加 `translate_*` 列（供 chat-translate EF 后台配置翻译 provider/key/model/url）

### `065_info_hub_content.sql`
- `transport_tips`（rail/taxi/metro）、`common_phrases`（cn/pinyin/en/audio_url）、`dialect_phrases`（dialect/emoji/cn/pinyin/en/audio_url）
- 种子：5 常用语 + 7 方言（川/京/陕）+ 3 交通攻略

## 二、Edge Function `chat-translate`
- 新建 `supabase/functions/chat-translate/index.ts`：复用 `_shared/volcengine.ts` 的 `chatCompletion`（LLM 翻译，翻译系统提示词）+ CORS。
- 新建 `_shared/translate-settings.ts`：从 `app_settings` 读 `translate_model/api_url/api_key/max_tokens/timeout_ms/target_default`（5min 缓存），空则回落 VolcEngine 默认。
- `volcengine.ts` 的 `chatCompletion` 加可选 `apiKey` 覆盖参数（向后兼容），满足「翻译 key 后台可配」。
- 同句缓存（5min TTL，上限 500）；翻译失败回原文保证聊天不中断。

## 三、admin CMS 注册（`admin/js/schema.js` + `nav.js`）
- 注册 11 张新表：签证引擎 5 表、支付 3 表、`support_agents`、交通/常用语/方言 3 表。
- 新增导航分组：🛂 签证引擎、💳 支付助手、💬 客服管理、🧭 实用信息内容。
- 字段类型全部复用现有（enum/json/ref_country/ref_city/slug/string_list/bool/richtext）。
- 方言/常用语音频先用 `audio_url` 文本字段（上传至 audio-guides bucket 后填 URL）——通用表编辑器未通用支持 `audio_upload` 的写列逻辑（该逻辑为 audio_guides/sub_areas 专用），后续可扩展通用上传器。
- `node --check` 校验 schema.js / nav.js 语法通过。

---

## 四、预订留痕（iOS，Wave 2 首个，build 通过）

### 模型 `YOLO/Models/ItineraryModels.swift`
- `ItineraryActivity` 加 `kind: ActivityKind`(attraction 默认/hotel)、`hotelId`、`sourcePlatform`，沿用「只写非默认键」的显式 Codable——旧 payload 无这些键则解码为默认，**向后兼容**。
- 新增 `ItineraryActivity.with(name:detail:hasAudio:)` 无损复制器（保留 kind/hotelId/sourcePlatform/timeSlot）。

### 写路径 `YOLO/Features/Plan/PlanFlowViews.swift`
- 新增 `HotelTripAdder`（days + addedHotelIds + add 闭包）。
- `BookYourTripView`：加 `working` 工作副本，酒店「＋加入行程」→ `confirmationDialog` 选第几天 → 写 `kind=.hotel`(hotelId/sourcePlatform="platform") activity → `saveItinerary`；已加入显示「已加入」。
- `HotelTripAdder` 经 `HotelSearchSheet → HotelSearchView → HotelCardView` 透传（可选参数，默认 nil 不影响他处）。

### 留痕卡 `YOLO/Features/Plan/BookingTraceCard.swift`（新建）
- 从 `days[].activities` 推导分组：🏨 酒店 / 📍 景点 / 🔗 外链（不留痕说明）。**不建新表**。
- 集成进 `ItineraryEditorView`（行程详情/编辑页）顶部 Section，有留痕才显示。

### 顺带修正（数据完整性）
- `ItineraryEditorView` 三个活动绑定改用 `act.with(...)`——原先重建 `ItineraryActivity` 会**丢 timeSlot**，加字段后还会丢 kind/hotelId。
- `ItineraryEditorView.save()` 补 `startDate/endDate/shareSlug/isShared`——原先重建 `SampleItinerary` 会**抹掉行程起止日期**（与既往修过的同类 bug 一致）。

### 验证
- `xcodebuild build`（generic iOS Simulator）**EXIT=0**，仅既有 warning。

---

## 五、签证引擎核心（iOS，纯逻辑层，build 通过）

先把最难、依赖最深的算法层锁定（UI 待接）。

### `YOLO/Models/VisaPolicyModels.swift`（新建）
- `VisaPolicy`/`VisaPolicyGrant`/`CityVisaTag`/`EntryPort`/`VisaRuleOverride`：camelCase 属性，依赖 Supabase 客户端的 `convertFromSnakeCase`（与现有 `VisaRule` 一致，无显式 CodingKeys）。DATE 列存 `String?`（"yyyy-MM-dd"）在引擎里解析，避开 PostgREST 日期解码坑。
- `VisaDataSet`（数据包）、`VisaQuery`（5 硬输入 + cities）、`OnwardStatus`、`VisaVerdict`（color/policyKey/secondaryMatches/blockers/latestExitDate/needsHumanReview，`isEnough` 计算属性）。

### `YOLO/Services/VisaPolicyEngine.swift`（新建，纯函数离线）
- `evaluate(query, data)`：GATE0 护照有效期 → 按入境日筛活跃 grants → 优先级升序遍历 P1–P5 命中即停（其余记 secondaryMatches）→ L 兜底 → 逐城 region 复核出 blockers。
- 过境第三地判定 `onwardStatus`（未定/往返同国/第三地，港澳算第三地）；P3 要求 grant+第三地+口岸 transit240h+停留≤上限；P4 要求海南口岸；P5 无团体输入不自动命中。
- 逐城复核：override 优先；无 override 时港澳默认免签、内地/特殊区随结论非红即免签。
- `latestExitDate` 解析 `entry_plus_Nd` clock_rule 或停留上限。
- 注：项目无 XCTest target，未加正式单测；DOC-02 四案例（SG/FR/US/IN）待补测试 target 后落地。`xcodebuild build` EXIT=0。

### 数据管线 + 检测器 + 结论卡（已接，build 通过）
- `YOLO/Services/VisaDataService.swift`（新建，仿 `PurchaseService.loadPlans`）：Supabase 并行拉 5 表 → 缓存（UserDefaults）→ in-code `bundledFallback`（6 国种子）兜底；`evaluate(query)` 便捷调引擎。`VisaDataSet` 加 `Codable`。接入 `AppEnvironment.visaData`。
- `YOLO/Features/Visa/VisaDetectorView.swift`：行程来源（读 selectedCityIds）+ **自测口子**（手动多选城市、仅本地不写回）+ 护照只读行 + 四选择器（出发/口岸/下一程 Menu、入境日 DatePicker + 停留 Stepper）+ 可选护照有效期 → 跑引擎弹结论。
- `YOLO/Features/Visa/VisaVerdictView.swift`：三色结论卡 + 城市 ✓/⚠️ chips + 最晚离境日 + secondaryMatches + blockers 提示 + 「看规则详情」展开 +「问真人确认」（Genius Bar 上线前为占位 alert）。
- `xcodebuild build` EXIT=0。

### 签证 × 路线 三路线（已接，build 通过）
- `YOLO/Services/VisaTripChecker.swift`：不够用时产出 R1 纯兴趣 / R2 办 L 签 / R3 签证友好。R3：国籍有 P3 资格 → **加香港作过境出口**激活 240h 过境免签（多一城）；否则去掉 blocker 城。
- `YOLO/Features/Visa/PlanRouteVisaCompareView.swift`：三路线对比卡，「采用」把城市写回 `selectedCityIds`（香港为过境出口建议，手动加末站）。
- 从 `VisaVerdictView` 的「看签证友好路线 →」按钮进入（不够用才显示）；检测器跑引擎时一并算好 routes 传入。
- `xcodebuild build` EXIT=0。

### 签证仍待接（非阻塞，后续）
- Plan 创建流程自动浮出 `.visaRoutePick`（够用静默/不够用插一步）——需改 `PlanCreateFlowView` 状态机，较侵入，暂以「检测器手动入口 + 结论卡看路线」替代。
- `VisaStatusCard` + Hero 只读签证摘要（需在 itinerary payload 落 visaAction 字段）。

---

## 六、支付助手 + Genius Bar + 实用信息中心（iOS，整批 build 通过）

### 导航地基
- `AppNavigation.AppModal` 加 `.infoHub/.visaChecker/.paymentHelper/.geniusBar` + present 方法；`MainTabView` fullScreenCover 加 4 分支。
- `HomeQuickAccessGrid` 第二格 Emergency → **实用信息**（Info Hub，紧急收入其中）；`HomeView` 接 `presentInfoHub`。
- 4 个新服务接入 `AppEnvironment`：`visaData`/`paymentHelper`/`supportChat`/`infoHub`。

### 支付助手（`Models/PaymentHelperModels.swift` + `Services/PaymentHelperService.swift` + `Features/Payment/PaymentHelperViews.swift`）
- 服务：Supabase 拉 3 表 + 缓存 + in-code bundled 兜底；3 问剪枝 `advice(dimension,country)`（match_json 匹配国家/排除卡组织/行程）；卡组织+行程类型本地持久化（UserDefaults）；`weChatBindingViable`。
- 三处境分流（未出发/已在中国/正卡住）→ 准备道（3 问 → 裁好方案 → 装App/绑卡(侧门→Genius Bar)/1元验证 → 随身支付卡进度）/ 救援阶梯（4 级可展开 + 个性化）/ 商家短语（`AVSpeechSynthesizer` 离线 TTS）。

### Genius Bar（`Models/SupportChatModels.swift` + `Services/SupportChatService.swift` + `Features/GeniusBar/GeniusBarViews.swift`）
- 服务：agents/conversations/messages 走 REST；**消息 3s 轮询**刷新（表已入 `supabase_realtime` 发布，后续可平滑换 Realtime）；图片上传 `chat-images`（私有 bucket，显示用 signedURL）；用户消息经 `chat-translate` EF 翻译为中文存 `body_translated`。
- 主页：客服 grid（online/busy 可点、绿/黄点）+ SOS（priority=emergency）+ 能帮/不能帮边界（待法务）；聊天：微信式气泡 + 译文 + 图片收发 + PhotosPicker。
- 注：决策为「仅 App 开着时收」，轮询契合；正式 Realtime 订阅作为后续优化。

### 实用信息中心（`Features/InfoHub/`）
- `InfoHubView`：6 卡 —— Genius Bar(置顶) / 紧急(复用 EmergencyView) / 签证(检测器) / 支付 / 交通 / 表达·方言。前四走 cover 切换，后两 push。
- `TransportView`：CMS `transport_tips` + 兜底，按 rail/taxi/metro。
- `PhrasesView`：常用语 + 方言彩蛋（横向方言切换 + 卡片 + **长按全屏大字模式** + 「听」）；`PhraseAudioPlayer`：有上传音频则 AVPlayer 播，无则 zh-CN TTS 兜底。
- `Models/InfoHubModels.swift` + `Services/InfoHubContentService.swift`（CMS + 缓存 + bundled 兜底）。

### 验证
- `xcodebuild build`（generic iOS Simulator）**EXIT=0**。签证检测器现经 Info Hub「🛂 签证」可达。

---

## 七、web/support 客服工作台（新建，自适应 PC/手机）

- `web/support/index.html`：单页控制台（原生 HTML/JS + supabase-js UMD CDN，复用 admin 色系）。
  - 登录 → 校验 `support_agents.user_id = auth.uid()`（非坐席拒绝）。
  - 顶栏「上线/忙碌/离线」三态手动切换（写 `support_agents.status`，App 端 grid 实时见绿/黄点）。
  - 会话列表（emergency 置顶，待接入可抢单）/ 聊天（译文显示、图片 signedURL、回复经 `chat-translate` 翻译为英文存 `body_translated`）。
  - 3s 轮询刷新（与 App 端一致）；**响应式**：宽屏两栏（列表|聊天），窄屏列表↔聊天全屏切换。
- 配置/部署：`config.example.js` + `scripts/build-config.mjs`（仿 web/，生成 `config.js`，已 gitignore）、`package.json`（build:support）、根 `package.json` 加 `build:support`/`deploy:support`、`wrangler.support.jsonc`（Cloudflare `yolo-support`）、`.github/workflows/deploy-support.yml`。
- `node --check` 构建脚本通过，`config.js` 生成验证 OK。

---

## 本日交付小结（均 build/校验通过并已推送 main）
- ✅ Wave1 数据地基（迁移 062–065 + chat-translate EF + CMS 11 表）
- ✅ 预订留痕（ItineraryActivity 扩展 + 酒店加入 + BookingTraceCard）
- ✅ 签证（引擎 + 数据管线 + 检测器/自测口子 + 三色结论 + 三路线对比）
- ✅ 支付助手（三处境 + CMS 分层 + 离线 TTS 短语）
- ✅ Genius Bar（客服 grid + SOS + 聊天：轮询 + 翻译 EF + 图片）
- ✅ 实用信息中心（6 卡 + 交通 + 表达/方言大字模式）+ 首页接入
- ✅ web/support 客服工作台 + 部署配置

## 八、客服系统商业化补强（多用户×多客服，build 通过）

让「用户能对每个客服持续聊 + 客服收到各用户发给自己的会话」真正可用：
- **一用户↔一客服 = 一条会话**：`startConversation` 先查同 (user,agent) 的 open 会话,有则**续聊**而非重复建（SOS 仍每次新建）。
- **用户端「我的会话」收件箱**：`loadMyConversations` + GeniusBarHomeView 顶部列出在进行的会话,点「继续聊」直接回到线程。
- **客服端看得清是谁**：建会话时把 `user_name/user_email` 写入 `context_json`；web 工作台会话列表显示**昵称 + 最后一条消息预览 + SOS 置顶**,聊天顶栏显示昵称+邮箱+签证/支付上下文。
- `xcodebuild` EXIT=0；web JS `node --check` 通过。

## 九、客服商业增强：未读 / 结束会话 / 正在输入 / APNs 推送（build 通过）

迁移 `066_support_chat_commercial.sql`：support_conversations 加 `user/agent_last_read_at`、`user/agent_typing_at`；新增 `device_tokens` 表(私有 RLS)。

- **未读数**：用户端「我的会话」每条显示未读红点(agent 消息 > user_last_read_at)；客服端会话列表显示未读徽章(user 消息 > agent_last_read_at)。打开会话即标记已读。
- **结束会话**：客服端聊天顶栏「结束会话」→ status=closed；双端会话列表只取 open,关闭后自动消失。
- **正在输入**：双向。输入时节流(~3s)写 `*_typing_at`；对端 6s 内视为「正在输入…」并显示。
- **APNs 离线推送**(代码完成,需 Apple 凭据才生效)：
  - `device_tokens` 表 + iOS `YOLOAppDelegate` 捕获 token + `AppEnvironment.enablePushRegistration()`(打开 Genius Bar 时注册,无权限则优雅降级不崩)。
  - Edge Function `notify-user`：ES256 签 APNs JWT(Web Crypto)→ 查 device_tokens → HTTP/2 推送;校验调用方是 support_agent。
  - web 客服回复后best-effort调用 `notify-user`。
  - **需你做**:Xcode 开 Push Notifications capability + aps-environment entitlement;创建 APNs Key(.p8)并设为函数密钥(APNS_KEY_P8/KEY_ID/TEAM_ID/BUNDLE_ID/HOST);`supabase functions deploy notify-user`。详见手动清单。
- 验证:`xcodebuild` EXIT=0；web `node --check` 通过。

### 唯一未切换项(如实)
- **消息刷新仍为 3 秒轮询**,未切 Supabase Realtime。原因:所有新功能(未读/输入态)已在轮询上正常工作;iOS RealtimeV2 API 切换有编译风险,放在会话末尾不赌绿色构建。需要「秒级实时」时单独做一轮(可保留轮询作兜底)。

---

## 十、完善后台与客服端网页

### web/support 客服端增强
- **客服可发图片**：📎 上传至 `chat-images` → 插 image_url 消息(此前用户能发图、客服只能回文字,已补齐)。
- **新消息提醒**：非当前会话来新用户消息时**提示音(WebAudio)+ 桌面通知(Notification API)**；登录时请求通知权限。
- **进行中 / 已结束 筛选**：列表顶部分段切换,可回看已结束会话(只看自己的)。
- **消息时间戳** + 既有(未读徽章/正在输入/结束会话/译文/图片)。

### admin 后台
- 新增**客服会话监控**：`support_conversations`、`support_messages` 注册为**只读**表(noCreate/noDelete),挂到「💬 客服管理」分组,供运营/QA 查会话与消息记录(RLS 已允许 is_admin 读)。
- `node --check` schema.js / nav.js / 客服端 JS 全通过。

---

## 十一、后台可视化：用真实数据下拉选择,不再手敲 ID

针对「建坐席要粘 auth UUID、签证适用要手敲 P1/P2」的痛点,新增两个**数据驱动选择器**(复用 ref_city/ref_country 那套):
- **`ref_user`**：从 `profiles` 拉账号,按邮箱下拉选 → `support_agents.user_id` 不再手粘 UUID。
- **`ref_visa_policy`**：从 `visa_policies` 拉政策,下拉选「P2 · 单方面免签 30 天」→ `visa_policy_grants.policy_key` 不再手敲。
- `ref-cache.js` 加 `users/policies` 缓存(容错:表缺失不影响其他选择器)+ `userLabel/visaPolicyLabel`;`field-types.js` 加两个 render + switch case;schema 改对应字段类型。
- 至此 admin 各模块基本「选已有数据」:城市/国家/口岸/政策/账号都是下拉;仅 `payment_advice_rules.match_json`(高级规则,设一次)仍为 JSON(带示例 hint),需要的话可再做成结构化多选。
- `node --check` ref-cache/field-types/schema/crud 全通过。

> ⚠️ 改的是 admin 静态站,需 `npm run deploy:admin`(或配 token 后 push 自动部署)才在线上生效。

---

## 仍需人工/后续
- [ ] 签证 6 国政策/城市/口岸数据**法务核对**（迁移内为起步数据）。
- [ ] `app_settings` 填翻译 provider/key/model；客服「能帮/不能帮」边界文案法务确认。
- [ ] Docker 起本地库 `supabase db push` 实测 062–065 + `supabase functions serve` 翻译往返 + 真机沙盒 RLS。
- [ ] 非阻塞增强：Plan 流程自动浮出 `.visaRoutePick` + `VisaStatusCard` + Hero 签证摘要；Genius Bar 由轮询切正式 Realtime；方言/常用语真人音频上传（CMS audio_url）；客服图片消息 App 端 signedURL 展示已支持。
