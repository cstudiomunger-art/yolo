# WORKLOG 2026-06-24 · Plan 创建流程接入签证判定 + 友好路线推荐

把签证判定与"友好路线"推荐**前移进 AI 推荐行程的创建主流程**:选完城市/日期后、生成行程前,按用户护照当场判定本次行程签证状况;不够用时给出经引擎复核的推荐路线(加过境 / 换城 / 删城)让用户选择确认。并新增"替换一座城"策略。

## 背景
原流程 `选城市 → 选日期 → 生成 → 预览 → 保存` 全程不判签证;签证能力只在行程**存好之后**以详情页一条 `VisaPlanHintBanner` 横幅出现(不够用才飘),点开才进检测器。判定引擎 / 粗判 / 路线生成 / 对比卡其实都已存在,本次主要是**接入时机前移** + 补"换城"算法。

## 改了什么

### 1. 新增"换城"策略 — `Services/VisaTripChecker.swift`
- 新增私有 `swapBlockers(query:appCities:catalog:data:rec:)`:取 `rec.blockers`(拖累城)→ 保留非拖累城,用候选城池 `catalog`(未在行程、可映射 admin code、且单独成行引擎判绿的城,按人气降序)等量回填,整组再调 `recommend` **复核为绿**才产出。文案"把 X 换成 Y,城数不变,经引擎复核全程免签"。
- `routes(...)` 新增 `catalog: [(slug:String, popularity:Int)] = []` 入参;由"只产一条 friendly(`??` 短路)"改为**收集多条**:加过境 + 换城;两者皆无时回落删城(至多 2 条 friendly)。排序按损失:`加过境(不丢城) > 换城(等量) > 删城(丢城) > 办 L 签`。
- `catalog` 缺省空 → 行为退化为现状,检测器结论页调用无需改。transit/swap/drop 标题区分("加一城过境/换城/去掉拖累城")。

### 2. 粗判单一真源 — `Services/VisaCoarseCheck.swift`
抽出 `query(citySlugs:start:end:countryCode:data:) -> VisaQuery?`,`recommendation(...)` 改为调它。让"判定的 rec"与"生成路线用的 query"是**同一份**,避免重复构造漂移。

### 3. 路线卡组件可复用 — `Features/Visa/PlanRouteVisaCompareView.swift`
- 新增可选 `onAdopt: ((VisaRoute)->Void)?`:默认仍写 `preferences.selectedCityIds`(检测器路径不变),创建流程可传闭包改本地草稿。
- 采用状态由 `VisaRoute.Kind?` 改为 `UUID?`(`adopted == route.id`)——现在可能有多条 friendly,按 Kind 追踪会一采全亮。

### 4. 创建流程插入签证步(主改动)— `Features/Plan/PlanCreateFlowView.swift`
- `enum Step` 增 `.visa`;日期步"Generate itinerary"按钮改为先进 `.visa`(`enterVisaCheck()`),不再直接生成。
- `enterVisaCheck()`:
  - 护照国为空或 `CN` → 直接生成(**静默跳过**:本引擎判的是外国人入境中国,CN 护照游国内不该误报)。
  - `await visaData.load()`,用**真实** `arrivalDate/departureDate` 经 `VisaCoarseCheck.query` 建查询 → `recommend`。
  - `isEnough`(绿/够用)或无可判城市 → 直接生成(**静默放行**)。
  - 不够用 → `VisaTripChecker.routes(..., catalog: cities.map{($0.id,$0.attractionCount)})`,展示结论头 + 路线卡 + 两个出口(「按推荐确认并生成」/「保持原行程继续」)+「精细核对」入口(present 现有 `VisaDetectorView` preset)。
- `adoptVisaRoute`:采用换城/删城更新本地 `selectedCityIds` 后生成;采用加过境(港澳为信息性、不进内容行程)城市集不变。
- 新增 `visaStep` / `visaVerdictHeader` / `visaRouteCard` / `cityDisplayName`(沿用 Theme token,视觉对齐检测器对比页)。

## 验证
- **编译**:`xcodebuild -project YOLO.xcodeproj -scheme YOLO -destination 'generic/platform=iOS Simulator' build` → **BUILD SUCCEEDED**。
- **引擎/策略回归**:扩展 `scripts/visa_live_test.swift`(swiftc 真引擎 + 线上 8 政策/201 grant/115 城/920 矩阵/28 口岸):
  - 原 A/B/C 全量基准不变;
  - 新增 **C2**:IN 散客(每城都需签证、无绿候选)→ 换城优雅返回 nil,不出换城卡;
  - 新增 **D1**(合成集):含拖累城 → 引擎复核把"乙市"换成"丙市",城数不变全程绿;
  - 新增 **D2**:全可达 → `isEnough` → routes 为空(静默放行)。
  - 结果 **12 通过 / 0 失败**。
- 说明:线上当前 6 座内容城市同质(均在单方面免签 + 240h 区),换城在真实数据上暂不会触发;待内容城市扩到免签区外才会出现"换城"卡。新算法以合成集验证。

## 改动清单
- `Services/VisaTripChecker.swift`:`swapBlockers` + `routes` 多 friendly + catalog 入参。
- `Services/VisaCoarseCheck.swift`:抽出 `query(...)` 单一真源。
- `Features/Visa/PlanRouteVisaCompareView.swift`:`onAdopt` 参数化 + 采用态按 id。
- `Features/Plan/PlanCreateFlowView.swift`:`.visa` 步 + 判定分流 + UI。
- `scripts/visa_live_test.swift`:新增 C2 / D1 / D2 用例。

## 追加 · 换城改为「列出候选城让用户自选」
原换城是自动按人气挑一座替换并直接生成。改为**展示所有符合条件的候选城,让用户自己选**。

- `Services/VisaTripChecker.swift`:
  - 退役自动 `swapBlockers`(及 `routes` 的 catalog 入参,`routes` 恢复为 加过境 / 删城)。
  - 新增 `SwapPlan`(blocker 名 / 保留城 / need 选几座 / `candidates` 候选城列表)+ `swapPlan(...)`:从候选城池筛出**未在行程、可映射、单独成行引擎判绿**的城,按人气排序返回;少于 blocker 数则返回 nil。
  - 新增 `verifySwap(query:keptSlugs:picks:data:)`:用户选定后,把保留城 + 所选城整组再调引擎复核必须为绿(引擎是裁判)。
- `Features/Plan/PlanCreateFlowView.swift`:
  - `enterVisaCheck` 改为算 `swapPlan`,并在有候选时从 `routes` 里隐去自动「去掉拖累城」(优先可选换城)。
  - 新增 `swapCard`:列出候选城为可勾选行(need=1 单选 / 多 blocker 多选,选满才可确认)→「按所选换城确认并生成」→ `verifySwap` 通过则写回 `selectedCityIds` 生成。
- `scripts/visa_live_test.swift`:D1/D2/C2 改测 `swapPlan`/`verifySwap`(D1 候选=丙市 need=1 verify=true;D2 plan=nil;C2 无绿候选 plan=nil)。
- 验证:`xcodebuild` BUILD SUCCEEDED;引擎回归 **12/0 通过**。

## 追加 · 选日期页加入签证输入(国籍/出发/下一程 + 有效期 + 已持签证)
让生成前的签证核对用真实输入,而非粗判默认。

### 国籍 / 出发国 / 下一程国(回程国)
- `Features/Visa/VisaDetectorView.swift`:`CountrySelectSheet` 由 private 改为 internal,供创建流程复用。
- `Services/VisaCoarseCheck.swift`:`query(...)` 增 `departure`/`onward` 覆盖(缺省=往返同护照国;onward="" 表示还没定→nil)+ `passportValidMonths` 入参。
- `Features/Plan/PlanCreateFlowView.swift`:选日期页新增「签证信息」卡——国籍/出发国/下一程三个可搜索国家选择行(复用 `CountrySelectSheet` + `ISO3166.all`),默认取自 `preferences.countryCode`;改国籍同步写回 `preferences.countryCode`。`enterVisaCheck` 用 natCode/depCode/onwardCode 构造查询(下一程是第三国/港澳→可触发过境免签)。

### 护照有效期(硬性判定 + 后台可编辑)
需求:有效期低于要求月数 → 既不能免签、也办不了签证(提示换发护照);要求月数后台可配,默认 3。
- **数据库**:`supabase/migrations/083_visa_config.sql` — 新建 `visa_config`(key/value,key=`passport_validity_months` 默认 3)+ RLS(公开读、管理员写)。
- **后台 admin**:`admin/js/schema.js` 增 `visa_config` 表定义(可改 value_int);`admin/js/nav.js` 在签证组加「引擎参数(护照有效期等)」入口;`admin/index.html` bump schema v40 / nav v35。
- **iOS**:
  - `Models/VisaPolicyModels.swift`:新增 `VisaConfigRow`;`VisaDataSet` 增可选 `config` + 计算属性 `minPassportValidityMonths`(缺省 3)。
  - `Services/VisaDataService.swift`:**独立 `try?` 拉 `visa_config`**(缺表/失败不拖垮主数据,避免级联回退);缓存 key v3→**v4**。
  - `Services/VisaPolicyEngine.swift`:GATE0 阈值由硬编码 6 改为 `data.minPassportValidityMonths`。
  - `Features/Plan/PlanCreateFlowView.swift`:选日期页加「护照有效期 ≥ N 个月」开关(N 取自 CMS,默认 3);不足 → 引擎判 GATE0 红,签证步显示「护照有效期不足…请换发护照」专用提示、不出路线(免签/签证都无效),仍可「保持原行程继续」。

### 续程机票(只提醒)+ 已持签证(跳过)
- `Services/VisaTripChecker.swift`:「加一城过境」路线 note 追加 ⚠️ 提醒订好续程/离境机票(过境免签硬要求)。不做输入开关。
- `Features/Plan/PlanCreateFlowView.swift`:加「我已持有中国签证」开关 → 勾选则直接生成、跳过整段核对。

### 验证
- `xcodebuild` BUILD SUCCEEDED;`node --check` admin JS 通过。
- 引擎回归台扩到 **14/0 通过**:A5 改 valid=2(默认阈值 3);新增 E1(默认阈值=3)/E2(后台改 6 → valid3 红 GATE0)。
- ⚠️ 待应用:`083_visa_config.sql` 同 078–082,需在 Supabase SQL Editor 手动跑(幂等)。未应用前端上 `visa_config` 拉取失败→回落默认 3,不影响主流程。
- 已应用并复核:线上 `visa_config` 返回 `passport_validity_months=3`(REST anon 读通)。

## 追加 · 修复签证步「线路无法选择」
反馈:签证核对页只有「加一城过境」绿卡有按钮,「纯兴趣」「备选·一城不动」两张卡看着像选项却点不了;且这两张其实是同一结果(原行程办 L 签),重复困惑。
- `Features/Plan/PlanCreateFlowView.swift`:
  - 每张线路卡都加可点按钮:友好路线填充绿底「按推荐确认并生成」,原行程描边「选这条 · 办 L 签生成」(均走 `adoptVisaRoute` → 写城市并生成)。
  - 去掉重复的「备选·一城不动」卡(`applyVisa` kind,与「纯兴趣」结果一致)。
  - 底部「保持原行程继续」改为仅在无任何线路卡时显示(GATE0/有效期拦截场景),其余情况由各卡自身按钮承担,消除冗余。
- 验证:`xcodebuild` BUILD SUCCEEDED。

## 追加 · 过境路线:港澳各出一张卡 + 文案动态化
原先「加一城过境」只出**第一个**命中的口岸(香港),且文案"240 小时 / ≤10 天"写死。
- `Services/VisaTripChecker.swift`:
  - `transitHubs` 加 `short`(香港/澳门)+ `landPort`(深圳/珠海)。
  - `activateTransit` 返回 `[VisaRoute]`——**对每个引擎复核为绿的口岸各出一张卡**(香港 + 澳门),不再短路只出一张;`routes()` 全部追加,无过境可行时才回落删城。
  - 卡片文案改为确认版三段式:`根据你的情况,把X加成途经/最后一站 → 符合<政策名>`;`想去X可经<深圳/珠海>陆路口岸或订机票`;`想去任意第三国(非出发国)同样免签,请重建行程把第三国设为下一程/返回国`;`⚠️ 需停留<动态>`。
  - 政策名取 `officialNameZh`、停留上限由 `stayLimitText` 从 `maxStayDefault/maxStayUnit` 动态生成(小时→天),与 CMS 同步,不再写死。
- 验证:`xcodebuild` BUILD SUCCEEDED;live harness 14/0;US 京蓉 8 天复现出「加香港过境(经深圳)」+「加澳门过境(经珠海)」两张卡,政策名=240 小时过境免签、停留=≤10 天 均动态。

## 追加 · 推荐城市落地行程(前向兼容钩子)
需求:后续 app 把港澳/海南等城市加进内容库后,推荐的城市要能进入实际行程规划。确认:加成最后一站并生成景点、现在就埋钩子、连海南/邮轮一起。
- `Services/VisaTripChecker.swift`:`VisaRoute` 增 `addedCitySlug`(港→`hongkong`/澳→`macao`);`transitHubs` 加 slug;过境卡带 `addedCitySlug`。
- `Features/Plan/PlanCreateFlowView.swift`:`adoptVisaRoute` 改为通用——采用路线时若 `addedCitySlug` 已存在于内容城市库(`cities`)就并入 `selectedCityIds` 作为真实一站、参与 AI 生成;**不存在则维持纯建议(no-op),前向兼容**。换城的替换城本就是内容城市,已天然进行程,无需改;未来海南/邮轮路线卡走同一套通用入行程逻辑。
- 说明:海南/邮轮属"换免签制式"(海南口岸 / 海港+随团),是新路线类型,且当前无对应内容城市无法生成/测试;通用入行程钩子已就绪,待三亚/沿海城市入内容库后,这些路线卡产出即自动接入。
- 验证:`xcodebuild` BUILD SUCCEEDED;live harness 14/0(无回归)。

## 追加 · 港澳过境合并为一张卡(双按钮)
反馈:港、澳两张过境卡内容雷同,合成一张、底部两个按钮(选香港/选澳门)。
- `Services/VisaTripChecker.swift`:`VisaRoute` 增 `transitOptions: [TransitOption]`(短名+slug);`activateTransit` 由"每口岸一张卡"改为**产出一张合并卡**——`addedCity="香港 / 澳门"`、note 合并(深圳/珠海各列陆路口岸)、`transitOptions` 带港澳两项。政策名/停留仍动态。
- `Features/Plan/PlanCreateFlowView.swift`:`visaRouteCard` 检测 `transitOptions` → 渲染**每口岸一个按钮**「选香港」「选澳门」(并排);新增 `adoptTransit(route,option)` 按所选口岸入行程(存在内容城市则并入,前向兼容)。非过境卡维持单按钮。
- 验证:`xcodebuild` BUILD SUCCEEDED;live 14/0;US 京蓉 8 天复现单张合并卡 + 选项 [香港(hongkong),澳门(macao)]。

## 追加 · 武侯祠内容入库 SQL
- `scripts/gen-wuhou-sql.py`:读 `~/Desktop/武侯祠/EN_解说词-武侯祠/` 10 篇英文解说,markdown→富文本 HTML(`**粗**`→`<strong>`、`-`→`<ul>`、段落→`<p>`),拼出幂等 INSERT。
- `supabase/migrations/084_seed_wuhou_shrine.sql`:1 个景点 `chengdu_wuhou_shrine`(挂 `chengdu`,门票/开放/地址/贴士/地铁,`audio_guide_count=10`)+ 10 个子区域 `chengdu_wuhou_sa_01~10`,body=英文解说,`audio_url` 留空(后台再传)。全部 `ON CONFLICT DO UPDATE`。
- `085_migrate_wuhouci_media_to_shrine.sql`:把旧 `chengdu_wuhouci` 的封面/音频 URL 按中文名映射迁到新子区域(文件不动、共用同 URL),再删旧景点。
- 真实库自检:38 内容表 表名/排序列/读取全部正确,零不匹配。

## 重写 · 后台 CMS 迁移到 Vite + Vue 3(`admin-vue/`)
背景:老 `admin/` 无构建、16 个 `<script>` 手动 `?v=` 缓存号、8700 行全局、侧栏与内容区同步滑动、创建景点/上传报错。确认全量重写为 Vue 3 组件化、完整还原老树结构。
- **地基**:Vite + Vue3 + vue-router(hash)+ Pinia;Supabase 客户端 + `admin_users` 鉴权;`import.meta.env` 注入配置(内容哈希打包,**根除手动 `?v=`**)。
- **Schema 引擎**:`schema/tables.js`(老 `schema.js` 1:1 移植,38 表)驱动 `TablePane`/`RecordForm`/`TableList`;`FieldInput` 覆盖全部 ~40 字段类型——text/number/bool/enum/slug/tags/richtext(Quill)、ref_*(单/多选)、对象列表(`ObjectListEditor`:link/place/contact/help_phrase/flight_platform/segment/visa_detail/practical_info)、`ImageUrlList`(图库上传)、`PaymentMatch`、`ItineraryBuilder`、图片/音频上传(`lib/storage.js`,cover-images / audio-guides,路径规则与老版一致)。
- **折叠树侧栏**(`TreeNav`):城市→城市面板(按城市过滤的表)/景点→解说与详情 + 子区域,**手风琴(同级只开一个)+ 懒加载**;另 9 个顶层分组。侧栏与内容区独立滚动(修同步滑动)。
- **bespoke Hub 全还原**:`VisaWorkbench`(国别核对勾选矩阵 + grant 窗口编辑 + 政策定义卡 + 区域/口岸 chip 选择器 + 城市可达只读 + 快照)、`PricingHub`(景点+子区域付费档,即改即存+批量)、`MembershipHub`(计划/交易/退款)、`UsersHub`(订阅授权)。
- **景点音频**:`AttractionEditor` 在「解说与详情」内嵌**单条音频上传**(标题/id/主线路等自动处理,选文件即传即存,自动读时长),不涉子区域音频。
- 部署:`wrangler.admin-vue.jsonc`(目标 `yoloadmin-vue`,与老 `yoloadmin` 隔离,待 parity 后切流量)。
- 验证:`npm run build` 通过(830 模块);dev server HTTP 200 无错;38 表 + 视觉/视图、上传逻辑接线完成。老 `admin/` 保持不动。

## 追加 · 修复 App 上传缺图标(打包 TestFlight)
- 现象:Archive 后 Distribute → App Store Connect 上传失败,报 `90023`(缺 iPad 152×152 图标)+ `90713`(Info.plist 缺 `CFBundleIconName`)。
- 根因:`YOLO/Assets.xcassets/AppIcon.appiconset/` 仅有 `Contents.json` 且三个槽位(light/dark/tinted)均无 `filename`,实际没有任何 PNG → 资源编译产出空图标。
- 修复:用 `~/Desktop/logo.jpg`(1188×1188 无 alpha)经 `sips` 转 1024×1024 PNG 落为 `AppIcon.png`;`Contents.json` 改为单张 1024 通用图标(`idiom=universal`/`platform=ios`),Xcode 自动派生全尺寸(含 iPad 152)并写入 `CFBundleIconName`。
- 后续:需在 Xcode 重新 Archive 再 Distribute(构建号仍 1.0(1),上次未上传成功未占号)。

## 追加 · Info.plist 声明出口合规(免每次答加密问卷)
- 现象:TestFlight 上传后,App Store Connect 反复弹「App 加密文稿」(加密算法类型 + 是否在法国分发)。
- 处理:`SupabaseConfig.plist`(项目 `INFOPLIST_FILE` 基底)新增 `ITSAppUsesNonExemptEncryption = <false/>`。仅用 HTTPS/系统标准加密、可豁免,故声明不使用非豁免加密。
- 效果:下次 Archive 的新构建上传将自动跳过加密问卷。已上传的 1.0(1) 不受影响,本次仍需网页手动选(标准加密 / 法国分发=否)。
